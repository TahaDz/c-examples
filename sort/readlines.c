#include "readlines.h"


/////* getline: read lines from input && save it in an array*/
////int getline(void){
////	int c, i;
////	char line[];
////	for (i = 0; i < MAXLINE - 1 && (c=getchar)) != EOF && c != '\n'; ++i)
////		line[i] = c;
////	if (c == '\n') {
////		line[i] = c;
////		++i;
////	}
////	line[i] = '\0';
////	return i;
////}
////
////
/////* copy:  */
////
////void strcpy(char *s, char*t){
////
////	while(*s++ = *t++)
////		;
////
////}
////
